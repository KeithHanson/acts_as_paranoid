= dynamic_scope

Overrides some basic methods for the current model so that calling #destroy sets a 'deleted_at' field to the current timestamp.  ActiveRecord is required.

http://github.com/technoweenie/dynamic_scope